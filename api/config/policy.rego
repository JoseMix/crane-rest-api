package miPaquete

default allow = false

allow

{ input.role == 'admin'  }

allow

{ input.role == 'admin'  }

allow

{ input.role == 'admin'  }

allow

{ input.role == 'admin'  }

allow

{ input.role == 'admin'  }

